CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

PROJECT(hello_cuda CUDA)

SET(CMAKE_CUDA_STANDARD 14)

ADD_EXECUTABLE(hello_cuda test.cu)

SET_TARGET_PROPERTIES(hello_cuda PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
